# Route Library System — Live Demo

Browser demonstration of the C++ Route Library System.

The interface intentionally follows the console-style workflow of the original C++ project. It is a browser simulation for demonstration purposes, not a replacement web application.

Demo data is stored locally in the browser using localStorage.
